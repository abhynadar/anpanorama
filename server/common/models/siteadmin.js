'use strict';

module.exports = function(SiteAdmin) {
/*
  //How to create a remote method - this is for reference
  SiteAdmin.myLogin = function(email, password, cb) {
    console.log('Login requested: ' + email);
    SiteAdmin.findOne({where: {email: email}}, function(err, siteAdmin) {
      cb(null, siteAdmin);
    });
  };

  SiteAdmin.remoteMethod('myLogin', {
    http: {path:'/mylogin', verb:'post'},
    accepts: [{arg: 'email', type: 'string'},
            {arg: 'password', type: 'string'}],
    returns: {arg: 'siteAdmin', type: 'siteAdmin'}
  });
*/
};
